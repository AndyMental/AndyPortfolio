<% content_for :title, "Blog" %>

<% if notice.present? %>
  <p style="color: green" role="status" aria-live="polite"><%= notice %></p>
<% end %>

<h1>Blogs</h1>

<%= form_with url: blogs_path, method: :get, class: "search-form" do |form| %>
  <%= form.label :q, "Search blogs:" %>
  <%= form.text_field :q, value: params[:q], placeholder: "Keyword..." %>
  <%= form.submit "Search" %>
  <% if params[:q].present? %>
    <%= link_to "Clear", blogs_path %>
  <% end %>
<% end %>

<% if @blogs.empty? %>
  <section role="status" aria-live="polite">
    <h2>No posts yet</h2>
    <p>There aren't any blog posts to read right now. Check back soon.</p>
  </section>
<% else %>
  <div id="blogs">
    <% @blogs.each do |blog| %>
      <%= render blog %>
      <p>
        <%= link_to "Show this blog", blog %>
      </p>
    <% end %>
  </div>
<% end %>

<% if blog_admin? %>
  <%= link_to "New blog", new_blog_path %>
<% end %>
